Encode the source document into the Minimized Ladder Schema using the token-saving variant.

Requirements:
- Drop `S2`.
- Compress `L2.sf` so it contains only one entry per top-level section.
- Preserve the broad rhetorical path.
- Preserve the top-level structure.
- Preserve governing principles.
- Preserve the most important atomic detail clusters in `S1`.
- Do not include subsection-level rhetorical nuance unless it is essential.

Priorities:
1. Keep `L1` intact enough to recover shape.
2. Keep `L3` intact enough to recover author stance and thesis.
3. Keep `S1` strong enough to restore texture.
4. Shrink `L2` first.

Output only valid minimized ladder YAML.